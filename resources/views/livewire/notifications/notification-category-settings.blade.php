<div class="space-y-2">
    <flux:heading size="sm">
        <span class="inline-flex min-w-0 items-center gap-2">
            <flux:icon name="bell" variant="mini" class="size-4 shrink-0 text-sky-500/80 dark:text-sky-300/80" />
            <span class="min-w-0">{{ __('notifications.categories.'.$category) }}</span>
        </span>
    </flux:heading>
    <flux:text size="sm">{{ __('notifications.settings.category_helper') }}</flux:text>
</div>
